# URL of the deployed Modal backend (or local `modal serve` URL during dev)
MODAL_BACKEND_URL=https://YOUR-WORKSPACE--mimiccoach-healthz.modal.run

# Public URL the browser hits — usually /api/proxy on the same Vercel deployment
NEXT_PUBLIC_API_BASE=/api/proxy
