// Stub — full component lands in P6/P8.
// Sport / motion / skill_level / body_type filter controls.
"use client";

export function FilterBar() {
  return <div className="text-sm text-ink/60">Filter bar lands here.</div>;
}
